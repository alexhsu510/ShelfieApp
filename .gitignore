# Dependencies (Netlify installs these from package-lock.json at deploy time)
node_modules

# Local Netlify folder
.netlify

# Build output (Netlify runs `vite build` at deploy time)
dist

# Generated by `netlify dev` for the Deno-based edge bootstrap
deno.lock

# Local env / secrets
.env
.env.*
!.env.example

# Editor + OS noise
.DS_Store
*.log
